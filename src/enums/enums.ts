export enum StartValue {
  emptyСart = 0,
  accStartValue = 0,
  cardNumberLength = 19,
  expirationDateLength = 5,
  oneUnit = 1,
}
export enum NavigatePath {
  goods = '/test--shop-with-goods',
  cart = '/cart',
}
